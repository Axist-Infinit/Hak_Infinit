import requests

def search_vulndb(service, api_key):
    """
    Searches for vulnerabilities related to the specified service using VulnDB.

    :param service: The service name to search vulnerabilities for.
    :param api_key: The API key for authenticating with VulnDB.
    """
    print(f"Searching for vulnerabilities related to {service} using VulnDB...")
    headers = {'X-Api-Key': api_key}
    response = requests.get(f"https://vulndb
::contentReference[oaicite:0]{index=0}
 
